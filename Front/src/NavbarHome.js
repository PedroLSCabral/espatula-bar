

const NavbarHome = () => {
  return (
    <nav style={styles.nav}>
      <h2>Sistema</h2>
    </nav>
  );
};

const styles = {
  nav: { display: "flex", justifyContent: "space-between", padding: "1rem", background: "#008000", color: "#fff" }
};

export default NavbarHome;

